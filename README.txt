NOTE:
There are text files in the input/output folders in order to test the training, it's not supposed to be a specific dataset.

So, here's my journal of using AI to make AI:

Using a more specific prompt seems to have helped so far.

Installation seems to work with what GPT5 has generated.

I am going to attempt to run this code later.

I still need to create a dataset manually.

COOL, the code actually runs, though I think that it's broken.

I will examine the code now to see what it is actually doing.
